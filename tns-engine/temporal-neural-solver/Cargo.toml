[package]
name = "temporal-neural-solver"
version = "0.1.2"
edition = "2021"
authors = ["Temporal Neural Solver Team"]
description = "Ultra-fast neural network inference with sub-microsecond latency"
documentation = "https://docs.rs/temporal-neural-solver"
homepage = "https://github.com/temporal-neural-solver/tns"
repository = "https://github.com/temporal-neural-solver/tns"
license = "MIT"
readme = "README.md"
keywords = ["neural-network", "inference", "simd", "avx2", "performance"]
categories = ["science", "mathematics", "algorithms"]
exclude = [
    "benchmarks/standalone/*",
    "scripts/*",
    "validate.sh",
    "*.md",
    "!README.md",
]

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
# WASM support
wasm-bindgen = { version = "0.2", optional = true }
serde-wasm-bindgen = { version = "0.6", optional = true }
console_error_panic_hook = { version = "0.1", optional = true }
web-time = { version = "1.0", optional = true }
js-sys = { version = "0.3", optional = true }
web-sys = { version = "0.3", optional = true }

# Core numerical libraries
ndarray = "0.15"
ndarray-rand = "0.14"
nalgebra = "0.32"
rand = "0.8"
rand_distr = "0.4"
getrandom = { version = "0.2", features = ["js"] }

# Performance optimization
rayon = "1.7"
num-traits = "0.2"
core_affinity = "0.8"
libc = "0.2"

# CLI
clap = { version = "4.0", features = ["derive"] }

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Error handling
thiserror = "1.0"
anyhow = "1.0"

# Timing and benchmarking
criterion = { version = "0.5", optional = true }

# Additional dependencies for validation system
num_cpus = "1.16"
chrono = { version = "0.4", features = ["serde"] }

[dev-dependencies]
approx = "0.5"
criterion = "0.5"

[features]
default = []
benchmark = ["criterion"]
avx2 = []
avx512 = []
simd = ["avx2"]
wasm = ["wasm-bindgen", "serde-wasm-bindgen", "console_error_panic_hook", "web-time", "js-sys", "web-sys"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true

[[bin]]
name = "temporal-solver"
path = "src/bin/temporal-solver.rs"

[[bin]]
name = "tns"
path = "src/bin/tns.rs"

[[bin]]
name = "benchmark"
path = "src/bin/benchmark.rs"

[[bin]]
name = "comprehensive_benchmark"
path = "src/bin/comprehensive_benchmark.rs"

[[bin]]
name = "quick_validation"
path = "src/bin/quick_validation.rs"

[[bench]]
name = "optimized_benchmark"
harness = false